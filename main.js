/* const initApp = () => {
  const hamburgerMenu = document.getElementById("open-menu")
  const closeMenu = document.getElementById("close-menu")
  
  //Create a function that toggle open and close
  
  const toggleMenu = () => {
    closeMenu.classList.toggle('hidden')
    closeMenu.classList.toggle('flex')
  }
  
  //Adding event listener that list when the button is click and then toggle
  
  hamburgerMenu.addEventListener("click", toggleMenu)
  closeMenu.addEventListener("click", toggleMenu)
}


document.addEventListener("DOMContentLoaded", initApp) */